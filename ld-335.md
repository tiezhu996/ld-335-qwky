<!-- 改编自：医疗健康主题项目提示词/ld-335/ld-335.md；后端由 FastAPI (Python 3.11) 改造为 Go 1.22 + Gin + GORM -->
# ld-335 医保智能结算API网关

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**医保智能结算API网关（GbInsureAPI）** 为医院信息系统提供标准化的医保结算接口服务，支持参保人身份核验、费用上传、预结算、正式结算和结算单查询，对接医保局接口规范。项目类型：**纯后端 API 服务**（无前端页面/Nginx）。

### 2. 核心实体（≥ 3 个，必须贯穿后端）

- **ApiClient（调用方）**
  - 字段：id, name, client_type（HIS/THIRD_PARTY）, api_key_hash, role, status, rate_limit_qps, created_at
  - 关键关系：可发起多个 UploadBatch 和 SettlementOrder
  - 业务动作：注册调用方、分配接口权限与调用频率、校验 API Key
  - **此实体必须贯穿：数据库表 → backend/internal/model/api_client.go → backend/internal/repository/api_client_repository.go → backend/internal/service/api_client_service.go → backend/internal/handler/api_client_handler.go → backend/internal/router/api_clients.go**

- **InsuredPerson（参保人）**
  - 字段：id, id_card_no, medical_card_no, name, insurance_type（职工/居民/新农合）, insurance_status（在职/退休/居民医保）, insurance_place, personal_balance, created_at
  - 关键关系：被多个 Presettlement 和 SettlementOrder 引用
  - 业务动作：身份核验、参保状态查询、余额查询
  - **此实体必须贯穿后端，禁止在单一文件中处理 CRUD**

- **UploadBatch（费用上传批次）**
  - 字段：id, batch_no（自动生成）, client_id, insured_person_id, total_amount, item_count, upload_status（Validating/Validated/Duplicate/Failed）, created_at
  - 关键关系：隶属于 ApiClient，包含多个 FeeItem
  - 业务动作：接收费用明细、格式校验、重复性检查、生成批次号
  - **此实体必须贯穿后端**

- **FeeItem（费用明细）**
  - 字段：id, batch_id, item_code, item_name, item_type（药品/诊疗/耗材/检查）, unit_price, quantity, amount, self_pay_ratio, medical_category（甲类/乙类/丙类）
  - 关键关系：隶属于 UploadBatch
  - 业务动作：明细校验、重复检查、参与预结算
  - **此实体必须贯穿后端**

- **Presettlement（预结算）**
  - 字段：id, batch_id, insured_person_id, total_amount, insurance_pay_amount, personal_account_amount, self_pay_amount, deductible, reimbursement_ratio, result_payload, created_at
  - 关键关系：基于 UploadBatch 与 InsuredPerson 计算
  - 业务动作：预结算计算、多次预结算比对
  - **此实体必须贯穿后端**

- **SettlementOrder（结算单）**
  - 字段：id, settlement_no（唯一）, batch_id, insured_person_id, presettlement_id, status（Presettled/Settled/Reversed/Failed/PendingManual）, total_amount, insurance_pay_amount, settled_at, reversed_at
  - 关键关系：关联 Presettlement 与 InsuredPerson
  - 业务动作：正式结算、当日冲正（全额回退）、历史结算查询
  - **此实体必须贯穿后端**

- **DailyReconciliation（日终对账）**
  - 字段：id, reconcile_date, total_count, total_amount, success_count, fail_count, abnormal_orders, created_at
  - 关键关系：汇总当日 SettlementOrder
  - 业务动作：日终对账、异常结算单标记待人工处理
  - **此实体必须贯穿后端**

### 3. 核心 API 端点（≥ 4 个，至少 2 个端点复用同一 service/repository）

- **POST /api/v1/clients/verify**（参保人身份核验）
  - 消费实体：InsuredPerson
  - 关键交互：接收身份证号和医保卡号，返回参保状态、参保地、医保类型、个人账户余额
  - 复用：`InsuranceService`、`InsuredPersonRepository`

- **POST /api/v1/fees/upload**（费用明细上传）
  - 消费实体：UploadBatch、FeeItem
  - 关键交互：接收就诊费用明细，校验格式与重复性，生成上传批次号
  - 复用：`FeeService`、`FeeItemRepository`

- **POST /api/v1/presettlements/calculate**（预结算计算）
  - 消费实体：Presettlement、FeeItem、InsuredPerson
  - 关键交互：按医保目录与参保地政策返回分项明细，支持多次比对
  - 复用：`SettlementService`、`PresettlementRepository`

- **POST /api/v1/settlements/submit**（正式结算）
  - 消费实体：SettlementOrder、Presettlement
  - 关键交互：确认预结算后生成唯一结算单号并返回凭证信息
  - 复用：`SettlementService`、`SettlementOrderRepository`

- **POST /api/v1/settlements/{settlement_no}/reverse**（结算冲正）
  - 消费实体：SettlementOrder
  - 关键交互：结算当日全额回退
  - 复用：`SettlementService`、`SettlementOrderRepository`（与正式结算共用）

- **GET /api/v1/reconciliations/daily**（日终对账）
  - 消费实体：DailyReconciliation、SettlementOrder
  - 关键交互：返回当日总笔数、总金额、成功/失败笔数
  - 复用：`ReconciliationService`、`SettlementOrderRepository`

- **GET /healthz**（健康检查）与 **GET /swagger/*any**（OpenAPI/Swagger 文档）

### 4. 横切关注点（≥ 2 个）

- **认证授权（API Key + JWT 双认证）**
  - 触达文件层：
    - 数据库：`api_clients` 表含 api_key_hash、role 字段
    - 后端：`internal/middleware/auth.go`（JWT）、`internal/middleware/api_key.go`（API Key）、`internal/util/jwt.go`、`internal/util/api_key.go`
    - 路由：`backend/internal/router/*.go` 中全部业务接口需 `ApiKeyRequired()` + `JwtAuthRequired()`，不同调用方分配不同接口权限
    - 前端路径：无（纯后端，仅提供 Swagger 与 curl 调用说明）

- **接口限流**
  - 触达文件层：
    - 后端：`internal/middleware/rate_limiter.go`
    - 路由：结算、预结算、费用上传等接口按 ApiClient 限流
    - 配置：`internal/config/config.go`（默认 QPS）

- **审计日志**
  - 触达文件层：
    - 数据库：`audit_logs` 表
    - 后端：`internal/middleware/audit_log.go`、`internal/service/*.go` 埋点、`internal/constants/log_templates.go`

- **全局错误处理与参数校验**
  - 触达文件层：
    - 后端：`internal/middleware/error_handler.go`、`internal/util/app_error.go`、`internal/constants/error_codes.go`
    - DTO：`internal/dto/*.go` 使用 `github.com/go-playground/validator/v10`

### 5. 共享枚举/常量（≥ 2 组）

- **MedicalCategory（医保目录分类）**
  - 值：CLASS_A = 'class_a'（甲类）, CLASS_B = 'class_b'（乙类）, CLASS_C = 'class_c'（丙类）
  - 后端定义位置：`backend/internal/constants/medical.go`
  - 出现位置：FeeItem 模型、FeeService、SettlementService 预结算计算、日志模板、错误码、formatters
  - **README 必须列出该枚举在后端的所有出现位置**

- **SettlementStatus（结算状态）**
  - 值：PRESETTLED = 'presettled', SETTLED = 'settled', REVERSED = 'reversed', FAILED = 'failed', PENDING_MANUAL = 'pending_manual'
  - 后端定义位置：`backend/internal/constants/settlement.go`
  - 出现位置：SettlementOrder 模型、SettlementService 状态机、对账接口、日志模板、错误码、formatters
  - **README 必须列出该枚举在后端的所有出现位置**

- **InsuranceType（医保类型）**
  - 值：EMPLOYEE = 'employee'（职工）, RESIDENT = 'resident'（居民）, NEW_RURAL = 'new_rural'（新农合）
  - 后端定义位置：`backend/internal/constants/medical.go`
  - 出现位置：InsuredPerson 模型、身份核验接口、预结算报销政策分支、日志模板、错误码
  - **README 必须列出该枚举在后端的所有出现位置**

### 6. 共享后端 service/repository 清单（≥ 3 个，纯后端项目适配）

- `InsuranceService` + `InsuredPersonRepository`：被身份核验、预结算、正式结算端点复用
- `SettlementService` + `SettlementOrderRepository`：被预结算、正式结算、冲正、对账端点复用
- `FeeService` + `FeeItemRepository`：被费用上传、预结算、结算单明细导出端点复用
- `ApiClientService` + `ApiClientRepository`：被 API Key 中间件、限流配置、权限分配复用

### 7. 后端 util 清单（≥ 2 个，纯后端项目适配）

- `util/jwt.go`：JWT 生成与校验
- `util/api_key.go`：API Key 哈希与校验
- `util/settlement_calculator.go`：起付线、报销比例、个人账户支付、自费金额计算
- `util/formatters.go`：金额格式化、结算状态文本、医保类型文本、日期格式化

### 8. 后端中间件清单（≥ 2 个）

- `auth.go`：验证 JWT，将调用方/用户信息注入 gin.Context（`c.Set("client", ...)`）
- `api_key.go`：校验请求头 API Key 与签名
- `rate_limiter.go`：基于 ApiClient 的接口限流
- `error_handler.go`：统一错误响应格式
- `audit_log.go`：审计日志记录
- `request_logger.go`：请求日志（method、path、status、latency）

### 9. 技术要求

- **英文短名（用于 Docker Compose）**：`gbinsureapi`
- **后端端口**：19935
- **前端框架**：无（纯后端 API 服务）
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：PostgreSQL
- **ORM**：GORM
- **认证方式**：API Key + JWT 双认证（使用 `github.com/golang-jwt/jwt/v5`）
- **其他依赖**：`github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/postgres`、`github.com/gin-contrib/cors`、`golang.org/x/crypto/bcrypt`、`github.com/go-playground/validator/v10`、`github.com/swaggo/gin-swagger`、`github.com/swaggo/files`

### 10. 文件结构强制清单

本项目为纯后端服务，无前端目录。后端必须包含（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go              # 程序入口：加载配置、初始化 DB、装配依赖并启动 Gin
└── internal/
    ├── config/                  # config.go（数据库连接、JWT、API Key、限流、端口等）
    ├── model/                   # 按实体分文件：api_client.go, insured_person.go, upload_batch.go, fee_item.go, presettlement.go, settlement_order.go, daily_reconciliation.go
    ├── repository/              # 按实体分文件：api_client_repository.go, insured_person_repository.go, upload_batch_repository.go, fee_item_repository.go, presettlement_repository.go, settlement_order_repository.go, daily_reconciliation_repository.go
    ├── service/                 # 按实体分文件：api_client_service.go, insurance_service.go, fee_service.go, settlement_service.go, reconciliation_service.go
    ├── handler/                 # 按实体分文件：api_client_handler.go, insured_person_handler.go, upload_batch_handler.go, fee_item_handler.go, presettlement_handler.go, settlement_order_handler.go, daily_reconciliation_handler.go
    ├── router/                  # router.go + 按实体分文件：api_clients.go, insured_persons.go, upload_batches.go, fee_items.go, presettlements.go, settlement_orders.go, reconciliations.go
    ├── middleware/              # auth.go, api_key.go, rate_limiter.go, error_handler.go, audit_log.go, request_logger.go
    ├── dto/                     # 请求/响应结构体
    ├── constants/               # medical.go, settlement.go, error_codes.go, log_templates.go, messages.go
    └── util/                    # jwt.go, api_key.go, logger.go, formatters.go, app_error.go, settlement_calculator.go
```

**严禁合并职责到单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。**

### 11. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，本项目在规范分层的基础上，额外要求刻意制造合理的"屎山"耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装
   - 所有 handler、service、middleware 都必须引用 logger
   - 日志格式字符串必须集中定义在 `internal/constants/log_templates.go`，包含至少 25 条模板，如 `SETTLEMENT_SUBMIT_SUCCESS`、`PRESETTLEMENT_CALC_FAILED`
   - 任何业务字段变更（如 SettlementOrder 新增字段）都必须同步修改对应的日志模板和调用处

2. **异常信息必须分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message
   - 禁止在单一地方统一处理所有异常，必须在 handler 中再次包装 service 返回的错误
   - 错误 message 中必须包含实体名、字段名、角色名，如 `"SettlementOrder[no=GB20260816001] submit failed: fee batch not validated"`

3. **常量/工具类必须多处耦合**
   - `internal/util/formatters.go` 中同时包含金额格式化、结算状态文本、医保类型文本、医保目录分类文本、日期格式化
   - 多个 handler 和 service 都直接引用 formatters，而不是独立拆分
   - `internal/constants/messages.go` 同时包含接口返回文案、日志文案、错误提示文案
   - 修改一个文案可能同时影响接口返回、错误信息和日志内容

4. **结算状态机必须跨多处定义**
   - SettlementStatus 的流转规则在 service 状态机、冲正接口、对账汇总、日志模板、错误码、formatters 中同时存在
   - 新增一个状态值需要修改至少 10 处

5. **枚举必须多处重复定义且被多处引用**
   - MedicalCategory 和 SettlementStatus 必须在 constants、类型定义、日志模板、错误码、formatters、GORM 模型中同时存在
   - 新增一个枚举值时，必须修改至少 10 处文件

6. **牵一发动全身的验证标准**
   - 后续如果要求"给 FeeItem 新增一个 '材料' 项目类型"，必须触达：GORM AutoMigrate、FeeItem 模型、constants、FeeService、SettlementService 预结算、formatters、日志模板、错误码、费用明细校验、对账统计等 ≥ 10 个文件

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 12. 部署要求

这是纯后端项目，无前端页面/Nginx，通过 API 接口提供服务。

后端代码放在 `backend/`：
- 数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排后端、数据库
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=gbinsureapi`
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: gbinsureapi`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-gbinsureapi}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`（`/healthz`）
- 暴露端口：后端 `${BACKEND_PORT:-19935}:8080`，数据库 `5432:5432`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=gbinsureapi`
- 数据库连接相关：`DB_NAME=gbinsureapi_db`、`DB_USER=gbinsureapi_user`、`DB_PASSWORD=gbinsureapi_pwd`
- 后端认证：`JWT_SECRET=change_me_to_a_long_random_string`、`API_KEY_SECRET=change_me_to_a_long_random_string`
- 端口映射：`BACKEND_PORT=19935`、`DB_PORT=5432`

### 13. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配：
```
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
backend/coverage/
```

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 启动命令）
- API 访问地址和 Swagger 文档地址
- 技术栈
- 项目目录结构
- 环境变量说明
- API 调用示例（curl）
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + GORM`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`（或 `go run .`），构建命令为 `go build ./...`
- 提供 curl 调用示例（含 API Key 与 JWT 请求头）
- 不要保留脚手架默认 README 内容
- 必须包含"枚举出现位置清单"，列出 MedicalCategory 和 SettlementStatus 在后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 访问 `http://localhost:19935/healthz` 返回 200，`/api/...` 接口返回正常，`/swagger/index.html` 可打开
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿 model → repository → service → handler → router
7. 核心 API 端点 ≥ 4 个，至少 2 个端点复用 `SettlementService` 或 `InsuranceService`
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享后端 service/repository ≥ 3 个，后端 util ≥ 2 个
11. 后端中间件 ≥ 2 个（含 API Key 与 JWT 双认证、限流）
12. 文件结构强制清单已写入
13. 包含"严禁合并职责到单一文件"
14. 包含屎山代码设计要求
